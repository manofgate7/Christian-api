{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",
  "AIProvider": "openai", // or openrouter or azure_openai
  "OpenAICredentials": { //optional
    "ApiKey": "Not saved in repo -> when need to use update but don't check it in",
    "ApiHost": "https://api.openai.com/v1/chat/completions"
  }
}
